# Compatible peer input samples

Nine deterministic General Profile samples representing three providers with exact matching
country, machine, profile, protocol version, and protocol fingerprint. Run:

```bash
python -m cloudeyes_agent analyze examples/samples/peer-comparison-v1 \
  --output reports/provider-peer-comparison.json \
  --markdown reports/provider-peer-comparison.md
```
