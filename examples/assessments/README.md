# Assessments

- `provider-peer-comparison-v1.json` is a deterministic Provider Analytics schema 1.1 example with
  three strict compatible providers and one direction-aware metric comparison per provider.
- `provider-peer-comparison-v1.md` is the corresponding human-readable report.
- `assessment-example.json` remains the original foundation placeholder.
