# Provider

Provider-level evidence reports and Provider Analytics v1 aggregation.

The analytics layer builds one deterministic report per provider from strict compatible cohorts. It
preserves the existing evidence report and adds a multidimensional scorecard plus traceable
explanations. It never emits a universal provider score.
