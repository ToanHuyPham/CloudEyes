# Sample utilities

The sample package contains privacy and serialization helpers used before external transport.
Result Bundle v1 redacts credential-like keys plus URL user/query/fragment components from raw
JSON evidence while preserving the original local evidence file.
