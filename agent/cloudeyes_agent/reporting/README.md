# Reporting

Provider Analytics v1 supports deterministic JSON serialization and an optional Markdown renderer.
Markdown output is derived from the same analytics model and preserves rule IDs, evidence gaps, and
evidence references.
