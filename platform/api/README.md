# API

Backend Ingestion v1 exposes:

- `POST /v1/submissions`
- `GET /healthz`

The built-in server is intended for loopback development or operation behind a TLS reverse proxy. Provider comparison and public read APIs remain future work.
